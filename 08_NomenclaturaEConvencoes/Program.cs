/* Identificador é o nome que você atribui a um tipo, membro, propriedade, variável ou namespace.
 * User nomes claros e significativos.
 * Não criar nomes iguals, diferentes apenas por letras maiúsculas
 * Deve começar com uma letra ou sublinhado
 * Não pode iniciar com um número ou caractere especial.
 * Não pode conter espaços.
 * Podem usar caracteres Unicode. (não recomendado)
 * Não podem usar palavra-reservada da linguagem.
 * Não pode exceder 512 caracteres.
 * Usa a convenção CamelCase para variáveis, parâmetros e campos internos privados.
 * Usa a convenção PascalCase para classes, métodos, interfaces e propriedades.
 * Para constantes é recomendado usar letras maiúsculas.
 * (_) é usado para campos internos privados e somente para leitura.
 */
