//lê uma única linha de entrada do fluxo de entrada padrão. Retorna a mesma string.
Console.ReadLine();

//Lê apenas um único caractere do fluxo de entrada padrão. Retorna o valor ASCII do caractere.
Console.Read();

/* Lê apenas um único caractere do fluxo de entrada padrão.
 * Obtém a próxima tecla pressionada pelo usuário.
 * Retorna um tipo de ConsoleKeyInfo.
 * É usado para segurar a tela até que o usuário pressione uma tecla.
 */
Console.ReadKey();